package Game;

public class VideoPoker_sim {

	public VideoPoker_sim() {
		// TODO - implement VideoPoker_sim.VideoPoker_sim
		throw new UnsupportedOperationException();
	}
	
	/*
	private void hold(int[] cards) {
		int[] discarded_pos = new int[5];
		int index = 0;
		int n_cards_discarded = 0;
		for(int i = 0; i < 5; i++)
		{
			if(cards[i] == 1)
			{
				discarded_pos[i] = 0;
			}
			else
			{
				n_cards_discarded++;
				discarded_pos[i] = 1;
			}
		}
		player.discard(discarded_pos, dealer);
		
		dealer.draw(player, n_cards_discarded);
	}
	*/
}